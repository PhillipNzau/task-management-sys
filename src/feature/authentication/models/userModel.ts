// Define the User interface to represent user data
export interface User {
    id?: number;
    first_name?: string;
    last_name?: string;
    role?: string;
    email?: string;
    token?: string;
  }